<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/login.css">
    <title>Login Page</title>
</head>
<body>
    <div class="container">
        <h2>Log In</h2>
        <form action="login" method="post">
            <input type="text" name="name" placeholder="Enter Your Name" value="<%= name ? name : '' %>">
            <input type="password" name="password" placeholder="Enter Your Password">
            <input type="submit">
            <% if (messages.error) { %>
                <div class="alert-err">
                    <%= messages.error %>
                </div>
            <% } %>
            <% if (messages.success) { %>
                <div class="alert-success">
                    <%= messages.success %>
                </div>
            <% } %>
        </form>

        <a href="/signup">Sign Up your account</a>
    </div>
</body>
</html>